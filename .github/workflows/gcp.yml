name: Build and push Docker image

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-push-gcr:
    name: Build and Push to GCP
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: ${{ github.repository }}
      GCR_PROJECT_ID: ${{ secrets.GCR_PROJECT_ID }}
      GCR_HOSTNAME: ${{ secrets.GCR_HOSTNAME }}
      GCR_SERVICE_ACCOUNT_KEY: ${{ secrets.GCR_SERVICE_ACCOUNT_KEY }}
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - id: 'auth'
      uses: google-github-actions/auth@v1.1.0
      with:
        credentials_json: ${{ secrets.GCR_SERVICE_ACCOUNT_KEY }}
    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v1.1.0
    - name: Check gcloud CLI
      run: |
        gcloud info
    # - name: Build Docker Image
    #   run: docker build -t $IMAGE_NAME:latest .
    - name: Automatic Tagging of Releases
      id: increment-git-tag
      run: |
        bash ./scripts/git_update.sh -v major
    - name: Check Tag Version
      run: echo ${{ steps.increment-git-tag.outputs.git-tag }}
    # - name: Configure Docker Client
    #   run: |-
    #     gcloud auth configure-docker --quiet
    # - name: Push Docker Image to Container Registry (GCR)
    #   env:
    #     GIT_TAG: v0.1.0
    #     # GIT_TAG: ${{ steps.increment-git-tag.outputs.git-tag }}
    #   run: |-
    #     docker tag $IMAGE_NAME:latest $GCR_HOSTNAME/$GCR_PROJECT_ID/$IMAGE_NAME:latest
    #     docker tag $IMAGE_NAME:latest $GCR_HOSTNAME/$GCR_PROJECT_ID/$IMAGE_NAME:$GIT_TAG
    #     docker push $GCR_HOSTNAME/$GCR_PROJECT_ID/$IMAGE_NAME:latest
    #     docker push $GCR_HOSTNAME/$GCR_PROJECT_ID/$IMAGE_NAME:$GIT_TAG
    # - name: Push Docker Image to Artifact Registry
    #   env:
    #     GIT_TAG: ${{ steps.increment-git-tag.outputs.git-tag }}
    #   run: |-
    #     docker tag $IMAGE_NAME:latest asia-southeast2-docker.pkg.dev/$GCR_PROJECT_ID/images/$IMAGE_NAME:latest
    #     docker tag $IMAGE_NAME:latest asia-southeast2-docker.pkg.dev/$GCR_PROJECT_ID/images/$IMAGE_NAME:$GIT_TAG
    #     docker push asia-southeast2-docker.pkg.dev/$GCR_PROJECT_ID/images/$IMAGE_NAME:latest
    #     docker push asia-southeast2-docker.pkg.dev/$GCR_PROJECT_ID/images/$IMAGE_NAME:$GIT_TAG
